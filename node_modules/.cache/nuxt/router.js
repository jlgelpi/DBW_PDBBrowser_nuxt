import Vue from 'vue'
import Router from 'vue-router'
import { normalizeURL, decode } from 'ufo'
import { interopDefault } from './utils'
import scrollBehavior from './router.scrollBehavior.js'

const _1b793bf1 = () => interopDefault(import('../../../pages/blast.vue' /* webpackChunkName: "pages/blast" */))
const _2fd7bcff = () => interopDefault(import('../../../pages/search.vue' /* webpackChunkName: "pages/search" */))
const _4c43a394 = () => interopDefault(import('../../../pages/show.vue' /* webpackChunkName: "pages/show" */))
const _ffbc546a = () => interopDefault(import('../../../pages/index.vue' /* webpackChunkName: "pages/index" */))

const emptyFn = () => {}

Vue.use(Router)

export const routerOptions = {
  mode: 'history',
  base: '/',
  linkActiveClass: 'nuxt-link-active',
  linkExactActiveClass: 'nuxt-link-exact-active',
  scrollBehavior,

  routes: [{
    path: "/blast",
    component: _1b793bf1,
    name: "blast"
  }, {
    path: "/search",
    component: _2fd7bcff,
    name: "search"
  }, {
    path: "/show",
    component: _4c43a394,
    name: "show"
  }, {
    path: "/",
    component: _ffbc546a,
    name: "index"
  }],

  fallback: false
}

export function createRouter (ssrContext, config) {
  const base = (config._app && config._app.basePath) || routerOptions.base
  const router = new Router({ ...routerOptions, base  })

  // TODO: remove in Nuxt 3
  const originalPush = router.push
  router.push = function push (location, onComplete = emptyFn, onAbort) {
    return originalPush.call(this, location, onComplete, onAbort)
  }

  const resolve = router.resolve.bind(router)
  router.resolve = (to, current, append) => {
    if (typeof to === 'string') {
      to = normalizeURL(to)
    }
    return resolve(to, current, append)
  }

  return router
}
